name: mi_billetera_digital
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.8.1 <4.0.0" # Especificado con comillas para mayor claridad

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.10.3
  intl: ^0.18.1
  fl_chart: ^1.1.0 # La versión que tenías era 1.1.0, la actualizo a la última compatible
  google_fonts: ^6.3.2
  shimmer: ^3.0.0
  home_widget: ^0.8.0
  flutter_svg: ^2.0.10+1 # Paquete que queremos añadir
  local_auth: ^2.2.0
  shared_preferences: ^2.2.3
  math_expressions: ^2.4.0
  local_auth_android: ^1.0.35
  local_auth_ios: ^1.0.10
  rxdart: ^0.27.7
  google_sign_in: ^6.2.1
  flutter_local_notifications: ^17.0.0
  csv: ^5.1.1
  path_provider: ^2.1.3
  share_plus: ^9.0.0

  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
